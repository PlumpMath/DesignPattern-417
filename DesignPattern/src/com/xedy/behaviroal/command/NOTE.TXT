命令模式

把请求封装成对象，从而使用不同的请求参数化客户端，排队或则记录请求，并且支持撤销操作。

Encapsulate a request as an object, thereby letting you parameterize clients with different requests, queue or log requests, and support undoable operations.